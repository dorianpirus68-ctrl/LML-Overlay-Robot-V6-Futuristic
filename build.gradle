// Root build file - plugin versions declared in settings.gradle
// No legacy buildscript needed for modern Gradle + AGP

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
